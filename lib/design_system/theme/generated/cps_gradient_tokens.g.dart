// GERADO por Style Dictionary a partir de tokens/*.json (DTCG).
// NÃO editar à mão. Regerar: npm run tokens
import 'package:flutter/widgets.dart';

// ignore_for_file: type=lint

abstract final class CpfSeguroGradientConsts {
  static const LinearGradient brandLift = LinearGradient(
    begin: Alignment(-0.5, -1),
    end: Alignment(1, 0.5),
    stops: [0.0425, 0.8665],
    colors: [Color(0xFF0468BF), Color(0xFF2861FF)],
  );
}
