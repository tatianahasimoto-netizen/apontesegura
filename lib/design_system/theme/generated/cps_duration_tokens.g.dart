// GERADO por Style Dictionary a partir de tokens/*.json (DTCG).
// NÃO editar à mão. Regerar: npm run tokens
// ignore_for_file: type=lint

abstract final class CpfSeguroDurationConsts {
  static const Duration micro = Duration(milliseconds: 120);
  static const Duration short = Duration(milliseconds: 150);
  static const Duration medium = Duration(milliseconds: 250);
  static const Duration slow = Duration(milliseconds: 400);
  static const Duration deliberate = Duration(milliseconds: 600);
  static const Duration spinner = Duration(milliseconds: 700);
  static const Duration shimmer = Duration(milliseconds: 1500);
}
